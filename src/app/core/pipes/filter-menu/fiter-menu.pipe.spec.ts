import { FiterMenuPipe } from './fiter-menu.pipe';

describe('FiterMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
